package patrones.Observer;

public interface Observer {
	//AL OBSERVER LE LLEGA QUIEN HA REALIZADO UN COMBIO(PUESTO UN POST) Y EL CAMBIO(POST).
	
	   public void actualizar(String nombre, String mensaje);
	   
	}